
# YOU – Smart Health App - مخطط المشروع الكامل (نسخة Flutter)

## 🎯 نظرة عامة
تطبيق صحي ذكي متعدد المنصات (iOS, Android, Web) مبني باستخدام **Flutter** مع دعم الذكاء الاصطناعي من Google Gemini وقاعدة بيانات Supabase.

---

## 🏗️ البنية التقنية (Flutter Stack)

*   **الإطار (Framework):** Flutter
*   **اللغة:** Dart
*   **التوجيه (Routing):** `go_router`
*   **قاعدة البيانات:** `supabase_flutter`
*   **الذكاء الاصطناعي:** `firebase_ai` (لـ Gemini)
*   **إدارة الحالة (State Management):** `provider`
*   **الخطوط:** `google_fonts`
*   **الكاميرا والصور:** `camera`, `image_picker`
*   **الإشعارات:** `flutter_local_notifications`
*   **الإعلانات:** `google_mobile_ads`

---

## 📁 هيكل المشروع (lib)

```
lib/
├── main.dart                   # نقطة الدخول الرئيسية مع Providers
├── app_router.dart             # إعدادات GoRouter
|
├── screens/
│   ├── auth/
│   │   └── auth_screen.dart      # صفحة تسجيل الدخول/التسجيل
│   ├── onboarding_screen.dart    # صفحة التعريف بالتطبيق
│   ├── profile_setup_screen.dart # إعداد الملف الشخصي
│   |
│   └── tabs/                     # التبويبات الرئيسية
│       ├── tabs_layout.dart      # Layout التبويبات مع BottomNavBar
│       ├── dashboard_screen.dart # الصفحة الرئيسية (Dashboard)
│       ├── scanner_screen.dart   # ماسح الوجبات بالكاميرا
│       ├── coach_screen.dart     # المدرب الذكي (AI Chat)
│       ├── mealplan_screen.dart  # خطة الوجبات الذكية
│       ├── workout_screen.dart   # التمارين الرياضية
│       └── profile_screen.dart   # الملف الشخصي
|
├── components/                 # واجهات مشتركة (مثل BannerAd)
│   └── banner_ad_widget.dart
|
├── providers/
│   ├── theme_provider.dart       # إدارة السمات (فاتح/داكن/تلقائي)
│   ├── language_provider.dart    # إدارة اللغات (عربي/إنجليزي/تركي)
│   └── user_profile_provider.dart # بيانات المستخدم + المزامنة
|
├── services/
│   ├── supabase_service.dart     # Supabase client + Auth
│   ├── gemini_service.dart       # Google Gemini AI
│   ├── nutrition_service.dart    # حسابات التغذية (BMR, TDEE)
│   ├── notification_service.dart # الإشعارات
│   └── ad_service.dart           # خدمات الإعلانات
|
├── models/
│   ├── user_model.dart           # نماذج بيانات المستخدم
│   └── meal_model.dart           # نماذج بيانات الوجبات
|
├── constants/
│   ├── app_colors.dart           # ألوان التطبيق (فاتح/داكن)
│   └── app_localization.dart     # الترجمات
|
└── config/
    └── ad_keys.dart              # مفاتيح الإعلانات
```

---

## 🗄️ قاعدة البيانات (Supabase Schema)
(هذا المخطط لا يتغير وهو متوافق مع Flutter)
### جدول `users`
```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), name TEXT NOT NULL, age INTEGER, gender TEXT, height NUMERIC, weight NUMERIC, activity_level TEXT, fitness_goal TEXT, meals_per_day INTEGER DEFAULT 3, workout_time INTEGER DEFAULT 30, food_budget TEXT, body_fat NUMERIC, tdee NUMERIC, caloric_goal NUMERIC, created_at TIMESTAMP DEFAULT NOW(), updated_at TIMESTAMP DEFAULT NOW()
);
```
### جداول `meals`, `messages`, `workout_logs`, `workout_feedback` (سيتم إضافتها لاحقًا).

---

## 🚀 خطة التنفيذ خطوة بخطوة

### **المرحلة 1: إعادة الهيكلة ووضع الأسس (الأولوية الآن)**
1.  **حذف الكود القديم:** حذف الملفات والمجلدات القديمة التي لم نعد بحاجتها.
2.  **تثبيت الحزم الأساسية:** تثبيت `supabase_flutter`, `google_fonts`, `provider`.
3.  **إنشاء هيكل المجلدات الجديد:** بناء هيكل المجلدات كما هو موضح أعلاه داخل `lib`.
4.  **إنشاء نظام الألوان:** إنشاء ملف `constants/app_colors.dart` مع الألوان المحددة في المخطط.
5.  **تحديث `main.dart`:**
    *   إعداد `ThemeProvider` باستخدام `provider`.
    *   تطبيق الألوان والخطوط الجديدة على `ThemeData` (الوضع الفاتح والداكن).
    *   إعداد `Supabase` وإعداد `GoRouter` مبدئيًا.

### **المرحلة 2: المصادقة وإعداد الملف الشخصي**
1.  إنشاء شاشة `auth_screen.dart` لواجهة تسجيل الدخول/الجديد.
2.  ربطها بخدمة `supabase_service.dart` للمصادقة.
3.  إنشاء شاشة `profile_setup_screen.dart` لجمع بيانات المستخدم بعد التسجيل.
4.  حفظ البيانات في جدول `users` في Supabase.

### **المرحلة 3: بناء الواجهة الرئيسية (التبويبات)**
1.  إنشاء `tabs_layout.dart` الذي يحتوي على `BottomNavigationBar`.
2.  إعداد `GoRouter` للتبديل بين الشاشات الخمس الرئيسية (Dashboard, Scanner, Coach, MealPlan, Workout, Profile).

### **المرحلة 4: تطوير الميزات الأساسية**
1.  **Dashboard:** بناء واجهة عرض السعرات والماكروز.
2.  **AI Coach:** إعادة تصميم وتوسيع الشاشة الحالية.
3.  **Scanner:** دمج حزمة `camera` و `image_picker` وربطها بخدمة Gemini.
4.  **Meal Plan & Workout:** بناء الواجهات وربطها بالخدمات لإنشاء الخطط.
5.  **Profile:** عرض بيانات المستخدم والسماح بتعديلها وتغيير اللغة والسمة.

### **المرحلة 5: الميزات الإضافية والتحسينات**
1.  دمج الإعلانات (`google_mobile_ads`).
2.  تفعيل الإشعارات المحلية (`flutter_local_notifications`).
3.  تحسين الأداء وضبط الواجهات.
